package tests

import (
	"testing"
)

func TestOnTransactionCreated(t *testing.T) {
	//fake.InitTestFirestore(context.Background())
	//
	//factory := fake.NewServiceFactory(context.Background(), "testUser")
	//factory.
	//
	//err := functions.OnTransactionCreated(, )
	//
	//assert.Nil(t, err)
}
